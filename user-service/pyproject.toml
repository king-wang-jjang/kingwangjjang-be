[tool.poetry]
name = "user-service"
version = "0.1.0"
description = "user service"
authors = ["woody <woody@aicess.ai>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.12"
uvicorn = "^0.32.1"
fastapi = "^0.115.6"
graphql-core = "^3.2.6"
strawberry-graphql = {extras = ["fastapi"], version = "^0.258.0"}

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
