name: Django CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.9]

    steps:
    - uses: actions/checkout@v3
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
   
    # docker image 빌드
    - name: Build docker image
      run: docker build -t hwanju1596/kingwangjjang:0.0.1 ./kingwangjjang

    # docker hub 로그인
    - name: Login docker hub
      uses: docker/login-action@master
      with:
        username: ${{ secrets.DOCKERHUB_USERNAME }}
        password: ${{ secrets.DOCKERHUB_TOKEN }}

    # docker hub 퍼블리시
    - name: Publish to docker hub
      run: docker push hwanju1596/kingwangjjang:0.0.1
  run:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
      matrix:
        python-version: [3.9]
        
    steps:
    # WAS 인스턴스 접속 & 애플리케이션 실행
    - name: Connect to WAS & Execute Application
      uses: appleboy/ssh-action@master
      with:
        host: ${{ secrets.WAS_HOST }}
        username: ${{ secrets.WAS_USERNAME }}
        key: ${{secrets.SSH_KEY}}
        port: ${{ secrets.WAS_SSH_PORT }}
        script: |
          ls /usr/local/bin
          echo "${HOME}/.local/bin" >> $GITHUB_PATH
          docker version
          apt --help
          instll --help
          apt-get --help
          ipkg --help
          docker ps -a
      env:
        ACTIONS_STEP_DEBUG: true
    # # Django 데이터베이스 마이그레이션
    # - name: Django DB Migration
    #   run: |
    #     docker run -d --name kingwangjjang_migration \
    #       -e DJANGO_SECRET_KEY=${{ secrets.DJANGO_SECRET_KEY }} \
    #       -e DJANGO_DEBUG=${{ secrets.DJANGO_DEBUG }} \
    #       -e DB_HOST=${{ secrets.DB_HOST }} \
    #       -e DB_USER=${{ secrets.DB_USER }} \
    #       -e DB_PASSWORD=${{ secrets.DB_PASSWORD }} \
    #       -e DB_NAME=${{ secrets.DB_NAME }} \
    #       hwanju1596/kingwangjjang:0.0.1
    #     docker exec kingwangjjang_migration python manage.py migrate
    #     docker stop kingwangjjang_migration
    #     docker rm kingwangjjang_migration

    # # Django 테스트 실행
    # - name: Django Tests
    #   run: |
    #     docker run -d --name kingwangjjang_test \
    #       -e DJANGO_SECRET_KEY=${{ secrets.DJANGO_SECRET_KEY }} \
    #       -e DJANGO_DEBUG=${{ secrets.DJANGO_DEBUG }} \
    #       -e DB_HOST=${{ secrets.DB_HOST }} \
    #       -e DB_USER=${{ secrets.DB_USER }} \
    #       -e DB_PASSWORD=${{ secrets.DB_PASSWORD }} \
    #       -e DB_NAME=${{ secrets.DB_NAME }} \
    #       hwanju1596/kingwangjjang:0.0.1
    #     docker exec kingwangjjang_test python manage.py test
    #     docker stop kingwangjjang_test
    #     docker rm kingwangjjang_test
    
    # - name: Check Docker version
    #   run: docker version
    #   # WAS 인스턴스 접속 & 애플리케이션 실행
    # - name: Connect to WAS & Execute Application
    #   uses: appleboy/ssh-action@master
    #   with:
    #     host: ${{ secrets.WAS_HOST }}
    #     username: ${{ secrets.WAS_USERNAME }}
    #     key: ${{secrets.SSH_KEY}}
    #     port: ${{ secrets.WAS_SSH_PORT }}
    #     script: |
    #       run: echo "${HOME}/.local/bin" >> $GITHUB_PATH
    #       docker version
    #       apt --help
    #       instll --help
    #       apt-get --help
    #       ipkg --help
    #       docker ps -a
